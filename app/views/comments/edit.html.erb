<div class="container">

<p>
    <%= form_with url: "/users/#{@user.id}/parks/#{@event.new_park.id}/events/#{@event.new_event.id}/user_new_events/#{@event.id}/comments/#{@comment.id}", method: :patch, local:true do |form| %>
   
      <%= form.label :body%>
      <p><%= form.text_field :body%></p>

      <%= form.submit "Submit", class: "btn"  %>
    <% end %>
</div>
