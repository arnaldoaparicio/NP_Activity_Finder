<%@events.each do |event|%>
    <h1><p><%= event.name %></p></h1>
   <p> <%= event.description %></p>
   <p> <%= event.location %></p>
   <p> <%= event.fee_info %></p>
   <p> <%= event.time[0][:timestart] %></p>
   <p> <%= event.time[0][:timeend] %></p>
<%end%>